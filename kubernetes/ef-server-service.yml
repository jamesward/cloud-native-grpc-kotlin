apiVersion: v1
kind: Service
metadata:
  name: ef-server-service
  labels:
    app: ef-server-service
spec:
  type: LoadBalancer
  ports:
    - port: 9092
      name: grpc
      targetPort: 9092
  selector:
    app: ef-server